{#
/**
 * @file
 * Default theme implementation for Symfony Email.
 *
 * Variables:
 * - content: Email body content.
 * - params: Array of parameters that can be used to replace values in template
 *   text.
 *
 * @see template_preprocess_email()
 *
 * @ingroup themeable
 */
#}

{{ content }}
